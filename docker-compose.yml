version: '3.8'

services:
  project-editor:
    build: .
    container_name: openclaw-project-editor
    ports:
      - "3001:3001"
    environment:
      - EDITOR_PASSWORD=${EDITOR_PASSWORD}
    volumes:
      - ../:/projects
    restart: always
