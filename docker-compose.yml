version: '3.8'

services:
  project-editor:
    build: .
    container_name: openclaw-project-editor
    ports:
      - "3001:3001"
    env_file:
      - /secret/project-editor/.env
    volumes:
      - ../:/projects
    restart: always
